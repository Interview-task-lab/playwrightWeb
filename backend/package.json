{
  "name": "playwright-codegen-studio-backend",
  "version": "2.0.0",
  "description": "Playwright Codegen Studio — Backend API Server",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "db:up": "docker-compose up -d",
    "db:down": "docker-compose down",
    "playwright:install": "npx playwright install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "playwright": "^1.45.0"
  }
}
